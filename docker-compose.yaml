
version: "3.9"

services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fake_social
    ports:
      - "5432:5432"
    volumes:
      - /Users/lintran/postgres_data:/var/lib/postgresql/data

  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   restart: always
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - postgres
    # environment:
    #   DATABASE_URL: ${DATABASE_URL}
    #   JWT_SECRET: ${JWT_SECRET}
    #   JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
    #   NODE_ENV: production
    # command: >
    #   sh -c "npx prisma migrate deploy && npm run start:prod"

volumes:
  postgres_data: 